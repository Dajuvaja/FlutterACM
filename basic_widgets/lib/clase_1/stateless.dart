import 'package:flutter/cupertino.dart';

class WidgetWithoutState extends StatelessWidget {
  const WidgetWithoutState({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
